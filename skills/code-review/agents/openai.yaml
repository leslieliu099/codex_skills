interface:
  display_name: "Code Review"
  short_description: "Review changes for defects, regressions, and missing tests"
  default_prompt: "Use $code-review to review these changes for defects, regressions, and missing tests."
